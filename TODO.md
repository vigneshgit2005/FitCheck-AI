# TODO - FitCheck-AI (GitHub README + security)

- [ ] Update root README.md: add/refresh “Today’s performance/verification” section using repo-known metrics.
- [ ] Add docs/screenshots/{onboard,wardrobe,recommend}.png (files not present in repo yet in this workspace).

- [ ] Ensure UI screenshot links exist under `docs/screenshots/` (onboard.png, wardrobe.png, recommend.png). If missing, add them once provided.
- [ ] Remove hardcoded Firebase API key/config from `src/lib/firebase.ts`.
- [ ] Load Firebase config from environment variables (VITE_*) and document them in `.env.example`.
- [ ] Update `.gitignore` if needed to prevent accidental commits of `node_modules/`.
- [ ] Verify git status only includes intended files (no node_modules).
- [ ] Commit and push changes.

